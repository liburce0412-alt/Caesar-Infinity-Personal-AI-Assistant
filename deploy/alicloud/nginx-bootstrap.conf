# Only ACME validation is public until HTTPS cutover is verified.
server {
    listen 80;
    server_name campusai.campus3ai.xyz;
    location ^~ /.well-known/acme-challenge/ { root /var/www/campusai-acme; }
    location / { return 503; }
}
