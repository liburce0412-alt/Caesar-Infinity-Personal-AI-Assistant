# CampusAI glass, feed scopes and product film

## Authorized scope and success criteria

- Restore the profile cover's rounded outline without moving semantic text or hit targets.
- Tree Hole and Wish Wall independently select public approved content or the current user's own content (including private, pending and completed entries). Default to My content. Keep publishing private by default, with explicit public opt-in.
- Port the final desktop reference's pointer-following cyan/violet rim, independently randomized single-color aurora and irregular meteors to touch/hover, keeping one shared renderer, bounded effects and motion-off behavior.
- Verify Kotlin, affected unit/UI tests, lint, APK, rendered screens and interaction on an isolated capture environment. Preserve all existing uncommitted work and user data.
- Produce a cinematic Chinese product film from real composables with fictional local fixtures. First complete cut plus at least three watched, documented revision cycles; deliver editable source and BGM/SFX-only exports. No publishing is included.

## Findings

- ProfileHero's image sits outside the newly isolated material layer. Only that layer clips its rounded shape; the opaque cover paints square corners.
- Posts return the latest 50 visible records without a scope predicate. Wishes merge public and owned queries. Scope must be applied before each query's limit.
- Desktop reference read from task 01a06f80-784f-77c0-b333-6eec945c7003 and live outputs/LiquidDesktop/desktop.js. Latest accepted changes include per-component random red OR green aurora, a brighter filament curtain, random meteors and pointer-following cyan/violet rim. Desktop dock sizing/layout is platform-specific.
- At initial inspection no Android device or AVD is running. Android 35 x86_64 system image exists; prepare an isolated emulator for capture.

## Progress

User steering: aurora and meteors are dark-mode-only and must be adapted to Android. Four independent persisted choices: deformation, following rim light, aurora, meteors. The global motion setting remains the master switch.

- [x] Screenshot, linked task, current source and existing changes inspected.
- [x] Application implementation and validation: targeted Android tests, pixel clipping, preference persistence, lint, assembly and 70 local database/privacy assertions passed.
- [x] Frozen demo capture assets and feature coverage map.
- [x] First full cut and three revision cycles, with full MP4 and inspected frame sequences for V1–V4.
- [x] Independent film review: all four visual/interaction corrections verified on V4; final dual exports validated. Subjective audio listening remains unverified and is documented.

Latest film direction: rich animation and an exterior-to-interior camera move; lifestyle scenes are optional and omitted. The user cancelled viewing the X reference video. All five environments (Original, Ocean, Ultraviolet, Ember, Aurora), Classic/Fluid styles and light/dark modes are shown with actual native screenshots and recordings. V1–V4 full renders have been reviewed; corrections are documented in the film's qa/revisions.md. Earlier source scaffolding and the failed V4 render startup do not count as completed revisions. Both final exports have matching H.264 picture hashes, 3213 frames and 107.1 seconds. Final independent V4 checks verified synchronized wish states, approximately 35px core auxiliary glyphs, sufficient environment hold and all nine actual audio peak anchors at their specified frames. Local Android completion, simulator UI capture, arm64 candidate delivery and unverified physical-device acceptance remain distinct.
